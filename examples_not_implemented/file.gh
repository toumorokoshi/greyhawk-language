class File:

	fileDescriptor Int

	None write(msg String):
		__builtins__.write(fileDescriptor, msg, 3)

	String read(num Int):
		buffer := Char[num]
		__builtins__.read(fileDescriptor, buffer, num)
		return buffer

f := File(1)

f.write("foo")
// print(f.write("foo"))