class File:

	fileDescriptor Int

	None write(msg String):
		__builtins__.write(fileDescriptor, msg, 6)

	String read(num Int):
		// buffer Char[name]
		_handle.read(fileDescriptor, buffer, num)
		return buffer