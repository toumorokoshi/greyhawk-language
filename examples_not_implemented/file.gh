class File:

	fileDescriptor Int

	None write(msg String):
		__builtins__.write(fileDescriptor, msg, 6)

	String read(num Int):
		buffer := Char[num]
		__builtins__.read(fileDescriptor, buffer, num)
		return buffer

// stdin := File(0)
stdout := File(1)

stdout.write("foooo", 6)
// stdout.write(stdin.read(10), 9)